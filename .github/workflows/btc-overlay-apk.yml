name: Build Wantview V3.2 APK

on:
  push:
    branches: [ "wantview-v3-2" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: btc-floating-overlay-v1
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Wantview-v3.2-debug
          path: btc-floating-overlay-v1/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Upload source project
        uses: actions/upload-artifact@v4
        with:
          name: Wantview-v3.2-source
          path: |
            btc-floating-overlay-v1
            .github/workflows/btc-overlay-apk.yml
          if-no-files-found: error
